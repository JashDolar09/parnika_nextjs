export const kurtaSetImages = Array.from({ length: 14 }, (_, i) => ({
    src: `/images/product/kurta_set/kurta_set ${i + 1}.webp`,
    alt: `Kurta Set ${i + 1}`,
    width: 300,
    height: 420,
    caption: "Premium Kurta Set"
}));
